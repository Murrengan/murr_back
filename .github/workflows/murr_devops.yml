name: my_murr_ci

on:
  push:
    branches: [ murr_bot ]
    

jobs:
  my_first_job:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: 1 job MURR
        run: echo from first job

      - name: 2 job murr
        run: |
          echo ----------------------------
          echo test, and deploy your project.
          echo ----------------------------
